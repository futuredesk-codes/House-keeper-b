version: "3.9"

services:
  backend:
    build: .
    container_name: housekeeper_backend

    ports:
      - "5005:5005"

    env_file:
      - .env

    restart: unless-stopped

    volumes:
      - ./storage:/app/storage